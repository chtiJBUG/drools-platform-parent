<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:websocket="http://www.springframework.org/schema/websocket"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:util="http://www.springframework.org/schema/util"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
        http://www.springframework.org/schema/beans/spring-beans.xsd
        http://www.springframework.org/schema/websocket
        http://www.springframework.org/schema/websocket/spring-websocket-4.0.xsd http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context.xsd http://www.springframework.org/schema/util http://www.springframework.org/schema/util/spring-util.xsd">

  <context:component-scan base-package="org.chtijbug.drools.platform.runtime.servlet"/>

    <bean id="guvnorResource" class="org.chtijbug.drools.runtime.resource.WorkbenchKnowledgeResource"
          factory-method="createGuvnorRessource">
        <constructor-arg index="0" value="${guvnor.url}"/>
        <constructor-arg index="1" value="${guvnor.groupId}"/>
        <constructor-arg index="2" value="${guvnor.artifactId}"/>
        <constructor-arg index="3" value="${guvnor.version}"/>
    </bean>
    <util:list id="droolsResources" value-type="org.chtijbug.drools.entity.history.KnowledgeResource">
        <ref bean="guvnorResource"/>
    </util:list>
    <bean id="platformRunner" class="org.chtijbug.drools.platform.runtime.servlet.DroolsPlatformKnowledgeBaseJavaEE"
          init-method="initPlatformRuntime" destroy-method="dispose">
        <property name="ruleBaseID" value="${knowledge.rulebaseid}"/>
        <property name="webSocketHostname" value="${ws.hostname}"/>
        <property name="endPoint" value="${ws.endpoint}"/>
        <property name="webSocketPort" value="${ws.port}"/>
        <property name="platformServer" value="${jms.server}"/>
        <property name="groupId" value="${guvnor.groupId}"/>
        <property name="artifactId" value="${guvnor.artifactId}"/>
        <property name="version" value="${guvnor.version}"/>
        <property name="username" value="${guvnor.username}"/>
        <property name="password" value="${guvnor.password}"/>
        <property name="droolsResources">
            <list>
                <ref bean="guvnorResource"/>
            </list>
        </property>

    </bean>




  <bean id="jmsclient"
        class="org.chtijbug.drools.platform.runtime.servlet.historylistener.ServletJmsStorageHistoryListener"
        init-method="initJMSConnection">
    <property name="platformKnowledgeBaseJavaEE" ref="platformRunner"/>

  </bean>


</beans>