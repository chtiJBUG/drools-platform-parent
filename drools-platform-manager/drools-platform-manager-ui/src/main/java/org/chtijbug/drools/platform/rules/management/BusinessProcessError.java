package org.chtijbug.drools.platform.rules.management;

public enum BusinessProcessError {
    ENVIRONMENT_UPDATE_ERROR,
    NO_ENVIRONMENT_DEFINED,
    DEPLOYED_VERSION_FETCH_ERROR,
    VERSION_ALREADY_EXISTS
}
